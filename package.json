{
  "name": "artists-aid",
  "version": "1.0.0",
  "description": "ArtistsAid - Plataforma para artistas, periodistas y fotógrafos",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run frontend\" \"npm run blog\" \"npm run docs\"",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start",
    "blog": "cd blog && npm start",
    "docs": "cd docs && npm start",
    "install-all": "concurrently \"cd backend && npm install\" \"cd frontend && npm install\" \"cd blog && npm install\" \"cd docs && npm install\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "artists",
    "journalists",
    "photographers",
    "platform"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "firebase-tools": "^13.31.2"
  }
}
